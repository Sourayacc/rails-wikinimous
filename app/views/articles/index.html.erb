<div class = "letter">
<h1 class = "letter1">NEW articleS</h1>
<%= link_to "New article", new_article_path(@article), class: "a" %>
</div>
<ol>
  <% @articles.each do |article| %>
    <div>
    <li class = "banner"> <%= link_to article.title, article_path(article) %> </li>
    <p class = "h1"> <%= article.content%> </p>
    </div>
  <% end %>
</ol>
